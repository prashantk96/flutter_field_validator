name: flutter_field_validator_example

publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_field_validator:
    path: ../

flutter:
  uses-material-design: true